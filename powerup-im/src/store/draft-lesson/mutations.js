export function updateTitle (state, title) {
  state.title = title
}
export function SET_TITLE (state, title) {
  state.title = title
}
export function LOAD(state, draft) {
  // unpack the draft to prepare for editing
}
